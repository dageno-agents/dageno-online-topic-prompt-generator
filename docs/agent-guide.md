# Agent Guide V3

Read SKILL.md first. Follow runtime/contract.mjs and stage schemas when producing machine data.

Codex can execute research with native tools and its selected model; the independent CLI uses OpenRouter. Neither mode permits a static industry fallback.

Build the intent registry before clustering. Preserve both brand capabilities and industry demand, and retain source references. Run independent gap and semantic review, then execute runtime/cli.mjs qa on the full V3 artifact. Do not label mocked evaluation as live research.

For exports, keep the four-column import separate from full JSON. See references/v3-output-contract.md and references/v3-validation.md.

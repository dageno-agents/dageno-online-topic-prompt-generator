# V3 Evidence Schema

Evidence sources carry ID, URL, publisher, source type, retrieval time, excerpt and content hash. Source references are checked against the actual fetched registry. Missing model scores remain absent, not filled with passing defaults.

Read [the current V3 contract](v3-output-contract.md) before executing this step.
Legacy V2 quotas, numeric acceptance thresholds and schemas are retired.

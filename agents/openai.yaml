interface:
  display_name: "Dageno Topic Prompt V3"
  short_description: "Evidence-backed industry intent panels and Dageno import CSV"
  default_prompt: "Use $dageno-topic-prompt-generator to research this domain from fresh evidence, challenge its business and market interpretation, enumerate industry intent units, generate natural questions, run semantic and deterministic QA, and export a four-column Dageno CSV with the full JSON evidence panel."
policy:
  allow_implicit_invocation: true
